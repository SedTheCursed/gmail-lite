<h1 class="email_object"><%= email.object %></h1>
<div class="email_body"><%=email.body%></div>
<div>
    <a href="<%= email_path(email.id) %>" data-method="DELETE" data-remote>
        <button class="btn btn-danger"><i class="fas fa-trash-alt"></i> Supprimer</button>
    </a>
    <a href="<%= email_path(email.id) %>" data-method="PUT" data-remote> 
    <button class="btn btn-warning">Marquer comme non-lu</button>
    </a>
<div>