var section = document.querySelector("section")
section.innerHTML = ""
section.insertAdjacentHTML("afterBegin", "<%= j render "email", email: @email %>")
document.querySelector("a[href='/emails/<%= @email.id %>']").parentNode.parentNode.classList.remove("unread")